CREATE TABLE ORDER_DATE(
	ORDER_DATE_ID INT IDENTITY(0, 1) PRIMARY KEY,
	ORDER_DATE DATE NOT NULL
)

INSERT INTO ORDER_DATE
SELECT DISTINCT CAST(ORDER_DATE AS DATE) AS ORDER_DATE
FROM STAGING_TABLE
ORDER BY ORDER_DATE